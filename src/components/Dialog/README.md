```jsx static
const interactions = [
  <Interaction type="request" />,
  <Interaction type="response" />,
  <Interaction type="request" />,
  <Interaction type="response" />,
];

<Dialog interactions={interactions} />
```
