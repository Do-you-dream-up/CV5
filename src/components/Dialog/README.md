```jsx static
const interactions = [
  <Interaction type="request" />,
  <Interaction type="response" />,
  <Interaction type="request" />,
  <Interaction type="response" />,
];
```

```jsx static
<Dialog interactions={interactions} onAdd={onAdd} />
```
